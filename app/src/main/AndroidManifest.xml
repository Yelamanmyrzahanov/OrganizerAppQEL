<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="kz.djunglestones.organizerappqel">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_organizer_main"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_organizer_main"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning"
        tools:replace="android:allowBackup">
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.ChooseCompanyActivity" />
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

        <activity android:name=".Activities.BindingActivity" />
        <activity android:name=".Activities.EditCompanyActivity" />
        <activity android:name=".Activities.CreateEventActivity" />
        <activity android:name=".Activities.OrderTicketInfoActivity" />
        <activity android:name=".Activities.CreateTicketsActivity" />
        <activity android:name=".Activities.FreeTicketActivity" />
        <activity android:name=".Activities.ChoosePrivacyActivity" />
        <activity android:name=".Activities.PaidTicketActivity" />
        <activity
            android:name=".Activities.EventDescriptionActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".Activities.QRCodeScannerActivity" />
        <activity android:name=".Activities.PromoCodeActivity" />
        <activity android:name=".Activities.AddPromoCodeActivity" />
        <activity android:name=".Activities.ChoosePromocodeTicketActivity"></activity>
    </application>

</manifest>